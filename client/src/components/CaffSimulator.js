function CaffSimulator({remaining}) {
  return (
    <div>
      Hello! now You have {Math.round(remaining)} milligrams of caffeine remaining in your body.
    </div>
  );
}


export default CaffSimulator;
